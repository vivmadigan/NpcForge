---
description: Ask the librarian to drain docs/lessons/inbox into the curated lessons, update the indexes and the path-scoped lesson rules. Use when the inbox has entries or the user asks to tidy the lessons.
---

Delegate to `crew:librarian` with the task `curate`. Pass along any extra instruction here: $ARGUMENTS

When the librarian reports back, relay its "Flags for the user" section verbatim; those are decisions only a human should make. Summarise "Did" in one or two lines.
